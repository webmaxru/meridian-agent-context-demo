# Allow only Meridian's private reviewed registry, and require every dependency to be pinned.
name: Meridian approved agent context
version: "1.0.0"
enforcement: block
fetch_failure: block

dependencies:
  allow:
    - webmaxru/meridian-agent-context-registry
    - webmaxru/meridian-agent-context-registry/**
  require_pinned_constraint: true
